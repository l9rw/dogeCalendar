{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the calendar window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
